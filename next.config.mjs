/** @type {import('next').NextConfig} */
const nextConfig = {
	// compiler: {
	// 	removeConsole: {
	// 		exclude: ['error'],
	// 	},
	// },
	images: {
		formats: ['image/avif', 'image/webp'],
	},
}

export default nextConfig
